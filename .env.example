# Copy this file to .env and replace placeholders.
# Use URL-safe letters and digits for local database passwords.

DB_PASSWORD=REPLACE_WITH_OWNER_PASSWORD
APP_DB_PASSWORD=REPLACE_WITH_APP_PASSWORD
DEMO_PASSWORD=REPLACE_WITH_DEMO_PASSWORD

JWT_SECRET=REPLACE_WITH_RANDOM_SECRET
NODE_ENV=development
API_URL=http://localhost:3001